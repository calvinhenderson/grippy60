if SHIELD_GRIPPY60_LEFT

    # Name must be less than 16 characters long!
    config ZMK_KEYBOARD_NAME
        default "Grippy 60"

    config ZMK_SPLIT_ROLE_CENTRAL
        default y

    config ZMK_USB
        default y

    endif

if SHIELD_GRIPPY60_LEFT || SHIELD_GRIPPY60_RIGHT

    config ZMK_SPLIT
        default y

    config ZMK_BLE
        default y

    # config ZMK_POINTING
    #     default y

    config I2C
        default y

    endif
